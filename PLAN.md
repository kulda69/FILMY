# PLAN

Pracovní plán pro `FILMY`.

Smysl:
- zapsat, co je další krok
- stručně vysvětlit proč
- držet směr projektu, aby se nerozjel jinam
- mít jedno místo, které můžu průběžně odškrtávat

Jak to používat:
- Jiří sem může dopsat další kroky, důvody i poznámky ke směru
- já budu položky průběžně odškrtávat a dopisovat krátký checkpoint
- když se směr změní, upraví se hlavně sekce `Směr`

## Směr

- Backend-first.
- Neřešit teď HTML/Jinja dřív, než bude dost pevný datový a API základ.
- Appka má být lokální zdroj pravdy pro historii, watchlist, ratingy a dostupnost.
- Výchozí vyhledávání titulů stavět nad IMDb katalogem.
- IMDb není online API zdroj, ale lokální katalog z pravidelně stahovaných dumpů.
- Stejný princip identity platí i pro osoby, nejen pro tituly.
- Nad jedním datovým základem může později existovat více různých pohledů do UI.
- Plex je doplňkový zdroj dat, ne primární katalogová vrstva.
- Vývoj běží na MacBooku, ale cílové běhové prostředí appky má být `mac-mini`, kde trvale běží Plex.
- TMDB enrichment není pro celý IMDb katalog.
- Dohledávat jen to, co je pro uživatele právě relevantní: watchlist, rozkoukané, viděné, ručně otevřené detaily a navazující lidi/související tituly.
- Systém musí umět spolehlivě průběžně dotahovat další informace na pozadí podle uživatelských akcí, ne jednorázově „nasypat všechno“.

## Aktuální stav

- [x] Základ FastAPI + DuckDB běží.
- [x] IMDb katalog se čte z lokálních TSV a materializuje do aplikačních tabulek.
- [x] Trakt a IMDb importy jsou oddělené od živých lokálních tabulek.
- [x] Sjednocená lokální knihovna je v `app.user_*` a `app.watch_events`.
- [x] TMDB sync pro jednotlivé tituly existuje.
- [x] Read-only napojení na Plex funguje proti skutečnému serveru `kulda_mini`.
- [x] První Plex sync do databáze proběhl.
- [x] Interní TMDB backfill a materializace detail cache běží pod background supervisorem.
- [ ] Ověřit stejnou stabilitu background pipeline i v reálném provozu mimo AI orchestrace.

## Další kroky

- [x] Ověřit, proč `data/tmdb_backfill.log` zůstal jen na startu, a rozhodnout, jestli backfill doběhl, spadl, nebo se vůbec nespustil.
  Důvod: runner tehdy nepsal průběžný stav po jednotlivých titulech, takže batch působil zasekle už po `batch_start`; nyní loguje per-item průběh a je vidět, kde se enrichment zpomaluje nebo selhává.

- [x] Navrhnout a zavést spolehlivý průběžný enrichment runner se stavem/checkpointy.
  Důvod: budoucí appka nebude dohledávat jen metadata titulů z watchlistu, ale i navazující informace o režisérech, hercích a jejich dalších filmech podle konkrétních uživatelských akcí.

- [x] Sepsat cílový backend workflow pro lokální správu knihovny.
  Důvod: ať je jasné, co přesně znamená `viděno`, `watchlist`, `rating`, `in progress` a co z toho se ukládá kam.

- [ ] Rozhodnout, které admin endpointy jsou jen dočasné technické nástroje a které zůstanou jako stabilní interní API.
  Důvod: backend už narostl a je dobré oddělit pracovní/debug vrstvu od budoucí app vrstvy.

- [ ] Sjednotit write orchestrace nad DuckDB a zamezit paralelním write procesům.
  Důvod: opakovaně narážíme na kolize write locku mezi server startem, background pipeline a jednorázovými maintenance scripty; problém není jen `async`, ale hlavně více nezávislých writerů nad jedním souborem.

- [~] Vrátit prioritu na rychlost requestů pro domovku, search a detail.
  Důvod: koncept lokálního katalogu a filesystem cache dává smysl, ale interaktivní appka se zpomaluje tím, že search, detail a background enrichment jsou stále příliš těsně svázané.
  Checkpoint:
  - 2026-07-07: přidáno lehké timing logování pro `/`, `/search` a `/titles/{tconst}`, aby šlo porovnávat reálné request časy bez dojmologie.
  - 2026-07-07: drahý levenshtein / wide fallback už neběží v normálním `search` režimu a je vyhrazený jen pro explicitní `mode=wide`.
  - Další krok 1: zúžit search flow tak, aby lookup vracel jen lehké kandidáty a plný `get_title_presentation()` se skládal až pro vybraný výsledek a několik alternativ.
  - Další krok 2: odlehčit homepage `Suggestions` a `genre signal` karty tak, aby se synchronně neskládalo tolik prezentací titulů při jednom requestu.
  - Další krok 3: oddělit background write provoz od UI requestů jedním writer modelem nebo řízenou frontou.

- [ ] Ověřit přepínání `tmdb_primary_language` mezi `EN` a `CZ` na skutečné detail stránce.
  Důvod: config a čtecí vrstva jsou připravené, ale Jiří to zatím nechce ručně testovat; je potřeba později potvrdit, že se v UI správně přepíná primární jazyk a fallback.

- [ ] Přesunout scrollbar styling a další drobné UI helpery do společného shell CSS až později.
  Důvod: `Continue Watching` rail je teď funkční a stabilní; styling zatím necháváme tak, aby se do něj zbytečně nesahalo.

- [ ] Později navrhnout, jak `favorite_traits` vstoupí do doporučování a score.
  Důvod: nová vrstva typu `cerebral`, `dark`, `slow-burn` dává smysl jako jemnější preference než hrubé IMDb žánry, ale teď ji zatím jen sbíráme a nechceme předčasně vymýšlet výpočet.

- [x] Rozdělit akce v lokální nabídce na `Move to` a `Copy to`.
  Důvod: jeden titul může současně patřit do více uživatelských seznamů (`Watchlist`, `Koukni rychle`, `Mam`) a přesun by ho neměl automaticky mazat z původního seznamu tam, kde dává smysl kopie.

- [ ] Přidat později pohled „ukaž, co je ve více seznamech“.
  Důvod: duplicity mezi seznamy teď budou vědomě povolené a ručně spravované; později má smysl mít nad nimi kontrolní přehled.

- [ ] Doladit vazby mezi listy a doménové důsledky akcí typu `Watched`.
  Důvod: začínají se objevovat hraniční stavy mezi `Watchlist`, `Hot Watchlist`, `Watched` a dalšími seznamy; je potřeba sjednotit, co přesně která akce automaticky schovává, archivuje nebo nechává aktivní.

- [x] Zapsat a držet jednotné workflow pro detail titulu.
  Důvod: zatím hlavně sbíráme data; při dotazu na konkrétní titul musí appka okamžitě vědět, co všechno má dohledat, složit a ukázat jako jeden smysluplný detail.

- [x] Navázat z detailu titulu na detail osoby přes `Main cast`.
  Důvod: data i portréty lidí se už materializují na pozadí; další přirozený krok je udělat z herců klikatelné vstupy do znovupoužitelného detailu osoby.

- [~] Rozřezat `filmy/db.py` na menší moduly podle odpovědnosti.
  Důvod: soubor už je příliš velký na bezpečné úpravy; první krok je nechat `filmy.db` jako stabilní fasádu a přesouvat implementace po ověřených blocích do menších modulů.
  Checkpoint: hotový je modul pro osoby (`filmy/db_people.py`), modul lokální knihovny (`filmy/db_library.py`) a modul legacy zdrojů (`filmy/db_legacy.py`). Do legacy modulu už jsou přesunuté nejen veřejné Trakt/IMDb/Plex read+sync vstupy, ale i interní helpery `_sync_trakt_*`, `_sync_imdb_*` a `_sync_plex_*`. `filmy/db.py` teď funguje hlavně jako fasáda a drží shared helpery plus seed/migrační logiku.

## Rozhodnutí

- IMDb a Trakt importy jsou historický seed, ne hlavní zdroj pravdy.
- Živé uživatelské údaje mají být lokálně v appce.
- IMDb je základní katalogová vrstva pro vyhledávání, protože má nejširší pokrytí titulů.
- Primární identita titulu v systému je IMDb `tconst` typu `tt5611024`.
- Primární identita osoby v systému je IMDb `nconst` typu `nm0000233`.
- Plex může dodat další vrstvu lokálních dat o knihovně, přehrávání a stavu sledování, ale má zůstat jen doplňkovým ingest zdrojem.
- Plex integraci navrhovat s ohledem na to, že produkční běh nebude na stejném stroji jako vývoj, ale na `mac-mini`.
- TMDB metadata a providery bereme jako enrichment nad lokální knihovnou.
- TMDB není primární zdroj pro nalezení titulu, ale druhá vrstva po nalezení IMDb záznamu.
- Praktická výjimka: protože IMDb nemá přímé online API v tomto workflow, může se pomocně ověřovat existence titulu i v TMDB, ale vazba a hlavní identita má zůstat přes IMDb ID.
- Totéž platí pro režiséry, herce a další osoby: TMDB může dodat enrichment osoby, ale hlavní interní identita má zůstat přes IMDb `nconst`.
- Enrichment je demand-driven, ne plošný nad celým IMDb katalogem.
- Uživatelova akce nad detailem může vyvolat další enrichment frontu, například pro režiséra, herce a jejich související filmografii.
- Potřebujeme robustní background mechanismus pro průběžné dohledávání dat, protože tento vzor bude v appce opakovaný a dlouhodobý.
- Priorita je vyřešit logický a datový základ fungování; UI je až vrstva nad stabilní identitou, vazbami a enrichment workflow.
- Plex má smysl připojit i dřív jako pomocný zdroj podkladů pro mapování titulů a následný TMDB enrichment.
- Obrázky držet na filesystemu, ne v databázi.
- Prioritní jazyk TMDB je `en-US`, fallback `cs-CZ`.
- Primární jazyk obsahu a detailu je angličtina; čeština je doplňkový fallback tam, kde dává smysl.

## Workflow detailu titulu

- Vstup je konkrétní titul identifikovaný primárně přes IMDb `tconst`.
- První krok je vždy načíst lokální detail z katalogu a lokální knihovny, ne začínat dotazem na externí API.
- Pokud už lokálně existují katalogová, knihovní a TMDB data, detail se hned složí z dostupných informací bez čekání.
- Pokud některá enrichment data chybí, detail se má i tak zobrazit hned z toho, co už máme, a paralelně se má vyvolat background dohledání chybějících částí.
- Detail titulu má při běžném uživatelském dotazu skládat aspoň tyto bloky:
  - základní identita: název, originální název, typ, rok, runtime, žánry, rating, aliasy
  - lidský obsahový detail: stručné „o čem to je“, hlavní herci, tvůrce / režie, případně epizody nebo sezóny
  - lokální stav uživatele: viděno, naposledy viděno, watchlist, rating, zařazení v seznamech, případně progress
  - dostupnost a média: providery v ČR, poster, backdrop a další lokální assety
- Výchozí čtecí jazyk detailu je angličtina. Pokud existuje užitečný český doplněk, může se zobrazit vedle nebo jako fallback, ale nemá přepsat anglický základ.
- Detail titulu musí fungovat i jako textový datový výstup bez UI. To je pracovní minimální forma, podle které se později postaví skutečné rozhraní.
- Z detailu titulu se teprve odvozují navazující background úkoly: lidi, související tituly, další filmografie a podobně.
- Dotaz na titul tedy není jen „najdi řádek v databázi“, ale „slož nejlepší aktuální detail z lokálních dat a současně rozhodni, co ještě chybí a má se dohledat“.

## Budoucí pohledy nad daty

- Jeden z budoucích pohledů může být vztahový, inspirovaný staršími filmovými databázemi typu „stojím na filmu -> vidím režiséra a herce -> stojím na režisérovi -> vidím jeho další filmy -> stojím na filmu -> vidím další herce“.
- Tento pohled je ale jen jedna možná navigační vrstva nad databází, ne jediná budoucí forma UI.
- Vedle něj mohou existovat i jiné, méně systematické nebo více task-oriented pohledy, například obyčejné hledání, watchlist, historie, doporučení nebo detail jednoho titulu.

## Doplňkové zdroje

- Plex je doplňkový ingest zdroj.
- Krátkodobě dává smysl zkusit připojení už teď, aby přinesl další podklady pro mapování a TMDB dohledávání.
- Dlouhodobě může mít dvě role:
  - jednorázový nebo občasný bootstrap import knihovny/stavu sledování
  - průběžný sync nových událostí, například přes webhooky
- Ani při napojení Plexu se nemá měnit hlavní identita objektů v systému: tituly přes IMDb `tconst`, osoby přes IMDb `nconst`.

## Poznámky Jiřího

- tady v tom souboru máme napsano "Z detailu titulu se teprve odvozují navazující background úkoly: lidi, související tituly, další filmografie a podobně."
- dohledavani vsech informaci bude podle me velmi pomale. pomale = nepouzitelne
- měření ukázalo, že `detail.json` cache už pomáhá, ale hlavní brzda je pořád fuzzy search vrstva; později má smysl optimalizovat search, ne detail cache
# K uvaze a posouzeni
- co kdyby se infomrace jak jak se prubezne dohledavaji pro rychlost zobrazeni informaci zapisovaly do lokalniho souboru.
- je to takova mala inspirace soubory nfo ktere se drive pouzivaly
- pri vyhledani nazvu filmu (tak jak jsme implementovali i s moznymi preklepy) vime kod filmu podle imdb ve formatu tt.....
- stejne tak se jmenuji adresare v data - assets - tmdb
- ve stejnem adresari by se mohl vytvorit novy sobour s veškerymi detaly titulu
- ja bych navrhoval ve formatu dictonary nebo neco podobneho
- napriklad u rezie by byl samozrejme reziser, ale byl by uveden jako tuple ("nm0357333", "Paul Hamann")
- stejne tak u herců
- to by bylo obdobne uvedeno i ve strukture ktera se bude posilat sablone jinja2
- podle me je vyhoda v tom, ze staci najit spravny nazev a dalsi udaje si vytahnout z tohoto soubru přičemž:
  - pri kliknutí na rezisera už by se nehledalo jeho jmeno, bylo by hned zname jeho id podle imdb
  - pri kliknuti na herce by platilo to same
  - takze pri hledani co jeste stejny reziser natočil uz by se to hledalo jen podle jeho identifikatoru
- pripada mi ze touto cestou by se to dalo zrychlit.
- dohledavani informaci a vytvareni techto souboru by mohlo beze na pozadi po dlouhou dobu
- pri hledani filmu u ktereho tento pomocny soubor neexistuje by se to proste vyhledalo poprve
- u lokalnich uzivatelskych seznamu se zacina chovani narovnavat vic systemove:
  `Watched` ma byt chapano jako doménová akce, ne jen technický zápis `watch_event`
- aktualni prakticka konvence:
  `Watched` v UI znamena pridat titul do seznamu `Viděl jsem` a zaroven ho odebrat z prave otevreneho aktivniho seznamu
- `Viděl jsem` bereme jako alias pro user-facing watched list; nenechavat to jen jako domluvu v chatu, ale drzet to explicitne v kodu a workflow
- u seznamu jsme nově přidali `description`; vlevo se zobrazuje misto technickeho `list_kind` a vpravo pod nazvem vybraneho seznamu
- zalozeni noveho listu je pres minimalisticke `+` v hlavicce `Your Lists`; vytvoreny list se ma hned stat aktivnim vybranym seznamem
- editace popisu existujiciho seznamu patri do hlavicky prave vybraneho seznamu vpravo; `Edit` ma byt zarovnane horizontalne vedle badge s nazvem
- lokalni nabidka na subkartach se kvuli orezu scroll kontejneru presunula z `details` na nativni HTML `popover`; pokud se k tomu budeme vracet, neresit to uz jen posunem `top/bottom`
- dalsi systemovy krok pozdeji:
  vytahnout chovani `Watched` do jedne centralni servisni/doménové akce, aby vedlejsi dusledky nebyly roztrousene po UI routach
- po vizualni kontrole skutecne domovky bereme aktualni vysku prave sekce `watchlist` jako minimalni; pri dalsich upravach ji uz nezmensovat
- v domovce ma byt prava sekce s vybranym seznamem vzdy alespon na tomto minimu a zaroven dorovnana na vysku `Your Lists`, pokud je levy panel vyssi

## Checkpoint

- 2026-07-10: po přesunu projektu na externí disk už `ensure_database()` nespouští zbytečný full refresh IMDb katalogu jen kvůli změně absolutní cesty nebo `mtime`; refresh se teď řídí skutečnou změnou obsahu a při pouhém přesunu se jen přepíše manifest/meta.
- 2026-07-10: homepage a listy už nejsou závislé na starých absolutních `local_path` u TMDB assetů. Read vrstva nově preferuje `relative_path` a fallback helper ještě umí přečíst i starší asset záznamy se starou absolutní cestou, takže přesun mezi disky nerozbije postery ani portréty.
- 2026-07-10: výběr lidí pro background portréty je zúžený jen na osoby navázané na tituly v `app.user_list_items` a na explicitně oblíbené osoby (`affinity_rating > 0` nebo `is_favorite = true`). Už se nemají rozjíždět tisíce zbytečných portrait fetchů mimo reálně relevantní knihovnu.
- 2026-07-10: `metadata_pipeline` už si při každém cyklu nepřepočítává globální pending pro portréty a person detail cache. U portrétů bere jen velikost aktuálního target batch a u person detailů se po zpracovaném batchi znovu neprochází celý kandidátní prostor. Cíl byl zkrotit CPU spike po startu serveru.
- 2026-07-10: po posledním restartu log ukazoval výrazně menší práci (`person_portrait_pending` spadl na 8 a `person_detail_pending` na 0), ale při předchozích pokusech byl `run_metadata_pipeline` ještě chvíli velmi žravý na CPU. Další krok, pokud se to vrátí, je znovu změřit už čistě nový běh po uklidnění logu a případně úplně vypnout background person detail pass a nechat detail osob jen on-demand.
- 2026-07-10: background `person_detail` pass je teď opravdu dočasně vypnutý (`person-detail-batch-size = 0`), protože právě on roztáčel CPU přes extrémně drahý DuckDB dotaz nad filmografií osob. Detaily osob se tím pádem dál skládají jen on-demand při otevření konkrétní osoby; až se k tomu vrátíme, je potřeba tuhle materializaci nejdřív architektonicky zlevnit, ne jen znovu zapnout.
- 2026-07-10: `data/filmy.duckdb` byla fyzicky nafouknutá na cca `18G`. Samotný `VACUUM` velikost nesrazil, ale přepis do nové DB ano; aktivní `data/filmy.duckdb` je teď kompaktní kopie o velikosti cca `6.4G`, původní soubor zůstal jako `data/filmy_precompact_backup.duckdb`. Obsahově sedí alespoň kontrolní počty `app.catalog_titles = 1265895` a `app.title_credits = 14300072`.
- 2026-07-10: po debatě o architektuře vychází jako reálný kandidát úplný přechod z DuckDB na PostgreSQL. Důvod není primárně analytický výkon, ale vhodnější runtime model pro živou appku, background joby a souběžné zápisy. Zatím bez implementace; další krok má být bezpečnostní backup celého projektu a teprve potom případný technický audit migrace.
- 2026-07-10: bezpečnostní kopie `/Volumes/not_inserted/PycharmProjects/FILMY_copy` byla ověřena proti pracovnímu projektu; aplikační soubory i data jsou shodné, liší se jen dva PyCharm metadata soubory. Technický audit je uložený v `POSTGRESQL_MIGRATION_AUDIT.md`. Doporučení je nezačínat úplným big-bang přesunem: nejdřív sjednotit connection/transaction vrstvu, poté přesunout malá zapisovaná runtime data do PostgreSQL a DuckDB dočasně ponechat jako read-only IMDb katalog. Na Macu už běží PostgreSQL 14.12 z `/Library/PostgreSQL/14`; původně nebyl rozpoznán, protože jeho binárky nejsou v běžném `PATH`. Databáze FILMY ani její data do PostgreSQL zatím migrované nejsou.
- 2026-07-10: PostgreSQL Fáze 3 má fail-closed pořadí opravené: před `002` se povolí jen prázdné `app` nebo všech šest tabulek s přesným fingerprintem, `002` už neuděluje žádná práva a idempotentní `003` je aplikuje až po kontrole schématu. Offline testy (13), bootstrap check, runtime `--check` i jeden opakovaný živý import prošly. PostgreSQL zrcadlo má počty `8 / 1187 / 21108 / 252 / 262 / 87`, bez osiřelých `list_id`; aplikace stále používá DuckDB. `filmy_app` je least-privilege uvnitř `filmy`, nikoli deklarovaně izolovaná od celého clusteru; ACL jiných databází se neměnila.
- 2026-07-10: zahájena Fáze 1 bez změny databáze. Nový `filmy/database.py` centralizuje otevírání DuckDB spojení a společný bounded retry pro read/write akce; dosavadní helpery ve `filmy.db` zůstávají kompatibilní fasádou. `filmy/db_library.py` už neotevírá spojení přes interní atributy `filmy.db`, ale používá tuto sdílenou hranici. Další inkrement má převést stejným způsobem `db_people.py` a poté oddělit runtime repository rozhraní od katalogových čtení.
- 2026-07-11: připraven první reverzibilní PostgreSQL runtime řez pro `app.content_state`. Přibyl explicitní přepínač `runtime_content_state_backend` v `config.toml` a nová vrstva `filmy/runtime_postgres.py`; při hodnotě `postgres` umí `update_content_state`, detail čtení a `Continue Watching` brát `content_state` z PostgreSQL, zatímco výchozí a stále aktivní stav zůstává `duckdb`. Offline test `tests/test_runtime_postgres_content_state.py` prošel; další krok je ruční přepnutí configu a živé ověření v appce.
- 2026-07-11: TMDB target selection a background počty už nečtou `in_progress` natvrdo z DuckDB. `filmy.db.get_tmdb_enrichment_targets()` teď při `runtime_content_state_backend = "postgres"` skládá `in_progress` kandidáty z PostgreSQL a zbytek kandidátů dál z DuckDB, zatímco `background_jobs.py` a `run_tmdb_backfill.py` berou počty přes nový centrální helper `get_tmdb_target_counts()`. Přepínač v `config.toml` byl poté opravdu přepnut na `postgres`; další krok je živé ověření read/write smoke a potom rozšíření stejného vzoru na další runtime tabulky.
- 2026-07-11: živý cutover pro `app.content_state` je potvrzený. `config.toml` zůstává na `runtime_content_state_backend = "postgres"`, běžné read helpery (`get_content_detail`, `get_continue_watching_items`) fungují nad PostgreSQL a dočasný write smoke přes `filmy.runtime_postgres.update_content_state()` prošel včetně úklidu testovacího řádku. Smoke zároveň odhalil bug při prvním insertu `previewed/watched`, kde se neplnily `last_previewed_at` / `last_watched_at`; opraveno synchronně v PostgreSQL i DuckDB větvi, aby se semantika nerozjela.
- 2026-07-11: živý cutover pro `app.user_ratings` je rozběhnutý stejným stylem. Přibyl samostatný přepínač `runtime_user_ratings_backend`; po ověření PG read/write smoke byl přepnut na `postgres`. `set_user_rating` / `clear_user_rating` už zapisují do PostgreSQL, detail/library summary a hlavní listové read modely (`Hot Watchlist`, `user list page`, episode ratings na detailu série) umí při PG režimu overlaynout latest ratingy z PostgreSQL nad jinak stále duckdb katalogovými dotazy. Doporučovací SQL zatím stále čte ratingy z DuckDB a je další kandidát na navazující inkrement.
- 2026-07-11: ratingy už nejsou napůl ani v doporučovací vrstvě. `get_genre_score_source_rows()` a interní `compute_and_record_genre_scores()` při `runtime_user_ratings_backend = "postgres"` overlayují latest title ratingy z PostgreSQL nad duckdb zdrojovou sadou, takže i `genre_scores` a navázané suggestions berou stejné user ratingy jako detail/listy. Ověřeno offline testem i živým smoke na `tt0242795`.
- 2026-07-11: `watch_events` mají připravený i aktivní první PG řez. Přibyl přepínač `runtime_watch_events_backend`; po forced live read ověření a compare na `tt4159076` byl přepnut na `postgres`. Běžné read helpery `get_watch_history()`, watched/recently watched view a `library summary` už umí číst z PostgreSQL, přičemž katalog/postery dál skládají přes DuckDB. `record_watch_event()` a `record_watch_events_through_episode()` umí v PG režimu zapisovat do PostgreSQL a zároveň držet `content_state` v PG; samostatná archivace watchlistu zůstává dočasně jako oddělený DuckDB krok, takže přes oba backendy nejde o jednu fyzickou transakci.
- 2026-07-11: `user_lists` mají hotový první PG runtime řez a `config.toml` je přepnutý na `runtime_user_lists_backend = "postgres"`. Zápisy `set_watchlist_state`, `add_title_to_user_list`, `create_user_list` a `update_user_list_description` už jdou do PostgreSQL; čtení `get_user_list_items_page`, `get_hot_watchlist_page`, `get_local_library_status` a `library summary` skládají membership/listy z PostgreSQL nad duckdb katalogem/postery. Současně se opravila archivace watchlistu při `record_watch_event()` v PG režimu, aby watched titul mizel z PG watchlistu a ne jen z DuckDB. Následně byly doplněny i PG větve pro `delete_group_from_user_list`, `move_group_between_user_lists` a `copy_group_to_user_list`; offline testy (`tests/test_user_lists_postgres_overlay.py`) i živý smoke create/copy/move/delete/cleanup prošly.
- 2026-07-11: proběhlo dočištění zbylých runtime read-modelů mimo hlavní CRUD. `TMDB enrichment targets`, `title detail cache targets`, homepage suggestion pool, genre suggestion pool a relevant-people seed už nejsou natvrdo závislé na DuckDB kopiích `watch_events` / `user_lists` / `content_state`, když je příslušný backend přepnutý na PostgreSQL. Přibyl regresní test `tests/test_runtime_postgres_runtime_targets.py`; celý vybraný test set prošel (20/20) a živý smoke potvrdil funkčnost `get_tmdb_enrichment_targets()`, `get_title_detail_cache_targets()`, `_get_relevant_people_candidates()`, `get_home_suggestion_sections()` a `get_genre_suggestion_candidates()`.
- 2026-07-11: začala `Phase 4: full PostgreSQL app backend`. Phase 0 inventura ukázala, že po dřívějším cutoveru pěti hlavních runtime tabulek zůstávají jako hybridní app-state zejména `tmdb_*`, `import_*`, `favorite_*`, `genre_scores`, `search_recall`, `imdb_file_manifest`, `catalog_refresh_meta` a `local_seed_meta`, zatímco samotný IMDb katalog (`catalog_*`, `title_*_lookup`, `person_lookup`) je samostatný větší blok. První implementační řez proto rozšířil PostgreSQL schema/migrátor z původní malé runtime vrstvy na celý tento app-state blok mimo katalog.
- 2026-07-11: rozšířený migrátor byl živě spuštěn a ověřen. `migrations/postgresql/002_runtime_schema.sql`, `003_runtime_grants.sql` a `filmy/scripts/migrate_runtime_to_postgresql.py` teď pokrývají i `imdb_file_manifest`, `catalog_refresh_meta`, `tmdb_title_map`, `tmdb_title_details`, `tmdb_watch_providers`, `tmdb_assets`, `import_batches`, `import_rows`, `local_seed_meta`, `favorite_genres`, `favorite_traits`, `genre_scores` a `search_recall`. Přidán byl kompatibilní fail-closed upgrade path z dřívější 6-tabulkové PG baseline. Živý běh migrátoru dopadl OK včetně row-count checks a sample-row verification; PostgreSQL nyní obsahuje celý tento app-state snapshot. Aplikace ale stále část těchto cest runtime čte z DuckDB, takže další krok je runtime cutover malých state tabulek a poté TMDB/import read-write cesty.
- 2026-07-11: první runtime cutover v rámci `Phase 4` je hotový a aktivní. Přibyl přepínač `runtime_app_state_backend`; po offline testech (23/23 OK) byl přepnut na `postgres`. Běžné runtime cesty pro `user_people`, `favorite_genres`, `favorite_traits`, `genre_scores` a `search_recall` teď v PG režimu čtou/zapisují do PostgreSQL, zatímco katalogové joiny a výpočtové overlaye zůstávají nad DuckDB katalogem. Živý smoke prošel pro čtení oblíbených žánrů/traits, latest genre scores, idempotentní replace round-trip i přepis existující `user_people` affinity. Další krok je stejným stylem přepnout TMDB/import/meta cesty (`tmdb_*`, `import_*`, `imdb_file_manifest`, `catalog_refresh_meta`, `local_seed_meta`).
- 2026-07-11: další `Phase 4` řez je hotový pro import pipeline. Přibyl přepínač `runtime_import_backend`; po doplnění PG helperů v `runtime_postgres.py` a routingu v `create_import_preview()`, `get_import_batch()` a `commit_import_batch()` byl přepnut na `postgres`. Tím se srovnala dřívější nekonzistence, kdy import commit ještě uměl zapisovat `watch_events` do DuckDB, i když hlavní runtime už běžel nad PostgreSQL. Offline test set prošel (26/26) a živý smoke nad syntetickým Netflix CSV potvrdil PG preview/get/commit flow (`rows_total=1`, `rows_unresolved=1`, následný `committed=0`). Další krok zůstává TMDB + meta vrstva (`tmdb_*`, `imdb_file_manifest`, `catalog_refresh_meta`, `local_seed_meta`).
- 2026-07-11: začal TMDB řez pro `Phase 4`, ale zatím bez live přepnutí. Přibyl přepínač `runtime_tmdb_backend` a první PG CRUD/detail vrstva pro `tmdb_title_map`, `tmdb_title_details`, `tmdb_watch_providers` a `tmdb_assets`. `runtime_postgres.py` teď umí PG mapping/detail/provider/asset read-write helpery a `filmy.db` na ně umí routovat `upsert_tmdb_mapping`, `store_tmdb_payloads`, `get_tmdb_mapping`, `record_tmdb_asset`, `get_latest_tmdb_assets`, `get_tmdb_detail_locales` i `_fetch_tmdb`. Offline test set prošel (29/29). Přepínač ale zůstává na `duckdb`, protože několik listových read modelů (`db_library.py` a části `db.py`) stále tahá poster joiny přímo přes DuckDB SQL nad `app.tmdb_assets`; další krok je převést tyto joiny na PG-aware helper vrstvu a teprve potom TMDB backend přepnout live.
- 2026-07-11: TMDB runtime backend je teď přepnutý live na `postgres`. Dočištěny byly poslední poster-dependent read modely přes společný helper místo natvrdo-duckdb joinů (`get_continue_watching_items`, postgres větve `get_hot_watchlist_page` a `get_user_list_items_page`, plus watch-history/recently-watched helper v `db.py`). Současně byl předělán PG fallback pro `get_tmdb_enrichment_targets()` a `get_title_detail_cache_targets()` z naivních per-title připojení na dávkový PostgreSQL dotaz přes completion flags. Offline test set prošel (30/30). Live smoke potvrdil přímé TMDB čtení (`tt0133093` -> TMDB 603, lokality `en-US/cs-CZ`, assety `poster/backdrop`), funkční target/cache cesty a nepadnoucí UI read modely (`Continue Watching`, `Hot Watchlist`, `Recently Watched`).
- 2026-07-11: dokončen i poslední meta blok a `Phase 4: full PostgreSQL app backend` je tím uzavřená. Přibyl přepínač `runtime_meta_backend`; po doplnění PG helperů pro `imdb_file_manifest`, `catalog_refresh_meta` a `local_seed_meta` byl přepnut na `postgres`. `get_imdb_manifest()`, cache fingerprinty a `seed_local_library()` teď v PG režimu používají PostgreSQL jako source-of-truth pro refresh metadata a seed guard, zatímco samotný IMDb katalog (`catalog_*`, `title_*_lookup`, `person_lookup`) zůstává záměrně v DuckDB. Offline test set prošel (33/33). Live smoke potvrdil `refresh_state = (False, False)`, manifest o 7 zdrojích, platný PG refresh fingerprint a existující seed marker `initial_import_unification`.
- 2026-07-11: začala `Phase 5: full PostgreSQL catalog engine`. Přibyly migrace `migrations/postgresql/004_catalog_schema.sql` a `005_catalog_grants.sql`, které zakládají `raw.*` IMDb TSV tabulky i odvozené `app.catalog_*`, `app.title_*` a `app.person_lookup` přímo v PostgreSQL. Nový skript `filmy/scripts/rebuild_catalog_postgresql.py` umí načíst lokální `imdb/*.tsv` přímo do `raw.*`, po jednotlivých tabulkách commitovat a potom v PostgreSQL rebuildnout celý katalogový read model. Live běh je rozjetý; po opravě SQL helperu `normalize_match_key()` a po zavedení průběžných commitů už pipeline postoupila minimálně přes `title.basics`, `title.ratings`, `title.episode` a běží dál na velkých `title_akas` / následně `title_principals`. Současně přibyl oddělený přepínač `runtime_catalog_backend` a první PG read-path pro `search_catalog()`, zatím ponechaný na `duckdb`, dokud nebude hotové a ověřené plné katalogové naplnění.
- 2026-07-12: katalogový rebuild v PostgreSQL doběhl a `config.toml` je nově přepnutý na `runtime_catalog_backend = "postgres"`. Při live smoke se našel a opravil první PG bug ve `fetch_catalog_search_rows()` (`psycopg.errors.IndeterminateDatatype` při `NULL` parametrech; opraveno explicitními `::text` casty). Po opravě fungují title-search cesty nad PostgreSQL: `GET /api/catalog/search?q=matrix&limit=5` ~0.7 s a `GET /search?q=matrix` ~0.5 s. Současně se ale potvrdilo, že většina těžkých read-modelů ještě na PG katalog nepřešla: domovka `GET /` ~75 s, title presentation `GET /api/catalog/presentation/tt0133093` ~60 s, HTML detail `GET /titles/tt0133093` ~55 s a person lookup `GET /api/catalog/person/lookup/text?q=keanu%20reeves` ~60 s. Další krok je převést `get_content_detail()`, person lookup a homepage/listové query z DuckDB katalogových joinů na PostgreSQL-aware read vrstvu, jinak bude cutover funkčně správný, ale výkonově jen částečný.
- 2026-07-12: další `Phase 5` runtime řez převedl část detail/person/list overlaye z DuckDB katalogu na PostgreSQL helpery. Přibyly nové helpery v `filmy/runtime_postgres.py` pro title/episode lookup, aliasy, title people, person lookup a lehké list/card projekce. `get_content_detail()`, `get_title_presentation()` cache build, `_fetch_person_cache_source_detail()`, `_fetch_title_people()`, `_fetch_aliases()`, `_fetch_known_for_items()` a person lookup search vrstvy teď při `runtime_catalog_backend = "postgres"` používají přímo PG katalog. Po prvním řezu spadly časy zhruba na: person lookup ~39 s, title detail/API presentation ~33 s.
- 2026-07-12: přidány první PostgreSQL view nad katalogovým runtime: `app.latest_title_posters` a `app.catalog_title_cards`. Současně rozšířeny granty pro `filmy_app` o tato view a o read-only přístup na `raw.title_episode` + `raw.title_principals`, aby bohatší person/detail projekce nepadaly na permissions. `db_library.py` teď umí při PG katalogu použít nové helpery pro episode->series mapu, title cards a `Continue Watching` katalogové řádky bez DuckDB joinů. Live smoke po tomto řezu: `GET /api/catalog/person/lookup/text?q=keanu%20reeves` ~0.43 s, `GET /titles/tt0133093` ~19.1 s, `GET /` ~41.3 s. Title search zůstává rychlý (~0.7 s). Další krok je převést selected-list / hot-watchlist grouping z Python + DuckDB fallbacků na jeden PostgreSQL read-model, protože právě ten teď pravděpodobně drží domovku kolem 40 s.
- 2026-07-12: další optimalizační řez se ukázal být mimo samotný katalog. Přímé timing probe odhalily, že po PG katalogových změnách už homepage nedržel selected panel, ale hlavně `get_local_library_status()` (~19.8 s) a `background_supervisor.homepage_snapshot()` (~8.3 s). `get_local_library_status()` byl přepsán tak, aby v PG režimu nevolal celé page buildery jen kvůli countům (`watchlist`, `hot_watchlist`, `watched`, `recently_watched`), ale počítal je přímo z `fetch_active_user_list_items()`, `fetch_all_watch_events()`, episode->series mapy a title-card/proster projekce. Tím spadl na ~0.7-0.9 s. `background_supervisor.homepage_snapshot()` dostal krátkou TTL cache a nakonec i `stale-while-revalidate` režim, takže first paint homepage už nečeká na drahé `rglob(detail.json)` + TMDB target count. Výsledný live smoke po tomto řezu: `GET /` ~2.65 s, `GET /lists/watchlist` ~1.58 s, `GET /titles/tt0133093` při cold-ish requestu ~2.15 s a při opakovaném hitu ~1.18 s. Hlavní homepage cíl je tím prozatím splněný; další rozumný krok je zkusit stejným stylem zjednodušit ještě ostatní derived list/page cesty a zvážit, které pomocné snapshoty dát do dalších PG view/materialized view.
- 2026-07-12: další audit derived stránek ukázal nové bottlenecky mimo homepage: `GET /views/watched` ~17.1 s, `GET /views/recently-watched` ~24.0 s, `GET /views/hot-watchlist` ~24.6 s a `GET /views/suggestions` ~23.2 s. Přímý timing probe ukázal, že samotné data buildery už byly mnohem levnější pro `recent`/`hot` (`~1.1 s` / `~1.7 s`) a že hlavní data bottleneck je zejména `get_watched_page()` (`~16.1 s`). Proto byl další řez veden dvěma směry: (1) `_fetch_watch_view_page_from_postgres()` byl předělán z Python groupingu všech `watch_events` na nový čistý PostgreSQL helper `fetch_watch_view_page_rows()` nad `app.watch_events` + `app.catalog_episodes` + `app.catalog_title_cards`; (2) z derived pages (`/views/watched`, `/views/recently-watched`, `/views/hot-watchlist`, `/views/suggestions`, `/views/suggestions/traits`) byly odstraněny eager `launch_homepage_warmup(...)` volání, která pravděpodobně přidávala latenci bez hodnoty pro first paint. Tyto poslední změny jsou v kódu a prošly `py_compile`, ale živé host-side ověření po nich už v tomto turnu neproběhlo kvůli runtime usage limitu nástroje pro další escalated běhy.
- 2026-07-12: host-side ověření navazujícího řezu už proběhlo a dopadlo dobře. Samotný PostgreSQL watched builder po přepsání na `fetch_watch_view_page_rows()` spadl zhruba na `~1.07 s` a po dalším doladění na `~0.64 s`; `get_recently_watched_page()` je kolem `~0.07 s` a `get_hot_watchlist_page()` kolem `~0.40 s`. U suggestions se potvrdilo, že první velký zbytečný náklad byla `_build_genre_signal_cards()`, kde se pro každý žánr synchronně volalo několik `get_title_presentation()` jen kvůli `overview`; to bylo nahrazeno dávkovým helperem `get_title_overviews_for_tconsts()` / `fetch_title_overviews()` a samotné genre-signal karty tím spadly zhruba z `~5.1 s` na `~0.016 s`.
- 2026-07-12: poslední velký bottleneck suggestions byl ještě shortlist kandidátů. `_get_home_suggestion_candidate_rows()` už při PG runtime nečte shortlist z DuckDB, ale přes nový helper `fetch_home_suggestion_candidate_rows()` přímo z PostgreSQL nad `app.catalog_titles`, `app.tmdb_title_details`, `app.tmdb_watch_providers`, `app.watch_events`, `app.catalog_episodes`, `app.title_credits` a `app.user_people`. Při zavedení se našla a opravila funkční regrese: helper musí respektovat `tmdb_locale_order` z `config.toml`, jinak se trait matching rozbije na českých overview. Po opravě se zachoval původní obsah (`trait_matches` zůstaly `tt33517752` + `tt23724682`) a timings spadly přibližně na: shortlist `~0.61 s`, celé `suggestions` `~0.66 s`, `watched` `~0.64 s`, `recent` `~0.07 s`, `hot watchlist` `~0.40 s`. Další rozumný krok je znovu změřit celé HTTP route end-to-end a pak stejným stylem vytahovat další opakující se shortlisty/projekce do PostgreSQL view/helper vrstvy.
- 2026-07-12: end-to-end HTTP měření nad běžícím serverem na `127.0.0.1:8019` potvrdilo, že derived pages už nejsou problém: první pass `/` `~2.665 s`, `/views/watched` `~1.049 s`, `/views/recently-watched` `~0.463 s`, `/views/hot-watchlist` `~0.819 s`, `/views/suggestions` `~1.390 s`, `/search?q=matrix` `~0.368 s`, `/titles/tt0133093` `~1.297 s`; druhý navazující pass spadl ještě níž (`/views/suggestions` `~0.934 s`, detail `~0.590 s`, search `~0.156 s`). Rozpad homepage ukázal nový hlavní bottleneck v `get_local_library_status()` (`~2.02 s` při cold čtení), zatímco `selected_panel_page` byl už jen `~0.365 s`, `continue_watching` `~0.04 s`, `favorite_traits` `~0.007 s`, `latest_genre_scores` `~0.009 s` a `background_supervisor.homepage_snapshot()` prakticky `0 s`.
- 2026-07-12: homepage dostala další zrychlovací řez. `get_local_library_status()` nově používá krátkou TTL cache (`5 s`) a při PG runtime také nový PostgreSQL read model/helper `fetch_library_status_projection()`, který počítá `watchlist`, `hot watchlist`, `watched` a `recently watched` přímo v SQL nad `app.user_list_items`, `app.watch_events`, `app.catalog_episodes` a `app.catalog_title_cards`, místo aby se vše tahalo do Pythonu. Tím klesl cold čas `get_local_library_status()` zhruba na `~0.40 s` (projection samotná `~1.14 s`, ale s menším Python overheadem a následnou TTL cache je první homepage hit výrazně lehčí; druhý hit statusu je `~0 s`). Následné HTTP ověření ukázalo další posun: `/` `~1.051 s`, `/views/watched` `~0.735 s`, `/views/suggestions` `~0.866 s`. Prakticky tím homepage klesla pod hranici 2 s i v reálném requestu.
- 2026-07-12: `get_local_library_status()` už v hlavní live konfiguraci (`user_lists/watch_events/catalog -> postgres`) neotvírá DuckDB vůbec. Přibyl nový helper `fetch_library_status_snapshot()`, který v PostgreSQL vrací jedním read-modelem jak summary počty (`lists`, `list_items`, `ratings`, `favorite_people`, `watch_events`, `watchlist_items`, `watched`, `recently watched`, `hot watchlist`), tak grouped `item_count` pro základní user listy. Tím se odstranil zbytek mezivrstvy přes `fetch_active_user_list_items()` + `episode_series_map` jen kvůli homepage sidebaru. Ověření: snapshot samotný `~0.37 s`, `get_local_library_status()` cold `~0.71 s`, druhý hit `~0 s`; HTTP `GET /` `~1.20 s`, `GET /lists/watchlist` `~0.78 s`, `GET /views/watched` `~0.77 s`, `GET /views/suggestions` `~0.99 s`. Další rozumný krok je stejným způsobem dočistit i zbývající list-detail read modely (`get_user_list_items_page`) tak, aby se v PG runtime úplně odpojily od Python groupingu nad `fetch_active_user_list_items()`.
- 2026-07-12: `get_user_list_items_page()` už v PG runtime také neběží přes DuckDB + Python grouping aktivních položek. Přibyl helper `fetch_user_list_page_rows()`, který řeší grouped list detail rovnou v PostgreSQL nad `app.user_list_items`, `app.catalog_episodes`, `app.watch_events` a `app.catalog_title_cards`, včetně watchlist filtru na už watched tituly a pagination-ready total count. Ověření: `watchlist` page builder `~0.76 s`, menší custom/trakt list `~0.03 s`; HTTP `GET /lists/watchlist` zůstává kolem `~0.79 s`, homepage `~1.63 s`, watched view `~1.06 s`. Tím jsou homepage i hlavní listové čtecí modely v běžném provozu už prakticky celé na PostgreSQL; další rozumný řez jsou detail/presentation/cache helpery v `filmy.db`, které ještě otevírají DuckDB kvůli aliasům, cache fingerprintům a některým library-summary overlayům.
- 2026-07-12: detail/presentation cesta už v PG runtime neotevírá DuckDB jen kvůli tomu, že historické helpery měly signaturu s `conn`. `get_content_detail()` při `runtime_catalog_backend = "postgres"` nově skládá aliasy, TMDB payload, content-state a library summary přes stejné helpery bez DuckDB connection; `get_title_presentation()` používá stejný model i pro title cache fingerprint; `db_people.get_person_presentation()` dostal stejný PG short-circuit. Praktické ověření: `get_content_detail('tt0133093')` `~0.06 s`, `get_title_presentation('tt0133093')` `~0.07 s`, `get_person_presentation('nm0000206')` `~0.30 s`, HTTP `GET /people/nm0000206` `~0.06 s`, `GET /search?q=matrix` `~0.25 s`, `GET /titles/tt0133093` `~1.74 s`. Tím se potvrdilo, že hlavní backend read-model detailu už není svázaný s DuckDB; zbývající latence HTML detailu bude spíš kombinace dalších navazujících částí stránky / renderu než starý katalogový join bottleneck.
- 2026-07-12: dočištěny další PG-only runtime mutace v `db_library.py`. `set_person_affinity_rating()` už při PG katalogu čte `catalog_people` z PostgreSQL; `record_watch_events_through_episode()` při PG katalogu používá `fetch_series_episode_rows()` místo DuckDB; grouped list mutace `delete_group_from_user_list()`, `move_group_between_user_lists()` a `copy_group_to_user_list()` už v PG větvi neotevírají DuckDB jen kvůli rozbalení group položek. Smoke prošel: epizodní bulk-watch vrátil `watched_count=1` pro `tt0959621`, grouped copy/move/delete nad dočasnými custom listy prošly `1/1/1`.
- 2026-07-12: TMDB servisní target-selection dostal live PG short-circuit. Když jsou `tmdb/catalog/content_state/watch_events/user_lists` všechny na PostgreSQL, `get_tmdb_enrichment_targets()` a navázané `get_title_detail_cache_targets()` už nepotřebují DuckDB mezikrok pro kandidátní sadu ani pro `priority_tconsts`; title metadata a episode->series mapy berou přes PG helpery (`fetch_catalog_brief_rows()`, `fetch_episode_series_map()`). Ověření: helpery běží bez pádu, `get_tmdb_enrichment_targets(limit=10, include_complete=False)` a `get_title_detail_cache_targets(limit=10, include_ready=False)` v aktuálním stavu vrací `0` položek, `get_tmdb_target_counts()` vrací `(2141, 2141)` — tj. všechny aktuální kandidáty jsou považované za kompletní.
- 2026-07-12: dočištěna i poslední živá `Hot Watchlist` read cesta v `db_library.py`. PG větev už neskádá watchlist groupy přes `fetch_active_user_list_items()` + DuckDB poster join, ale používá nový helper `fetch_hot_watchlist_page_rows()` přímo nad PostgreSQL (`app.user_list_items`, `app.watch_events`, `app.catalog_episodes`, `app.catalog_title_cards`). Ověření: builder `get_hot_watchlist_page(limit=50)` first hit `~1.13 s`, second hit `~0.32 s`; HTTP `GET /views/hot-watchlist` `~1.85 s`, homepage `GET /` `~0.73 s`.
- 2026-07-12: title-presentation cache load už v PG runtime neotvírá DuckDB ani při cache hitu. `_get_title_presentation_cached()` při `runtime_catalog_backend = "postgres"` volá `_load_cached_title_presentation(None, tconst)` přímo s PG-aware fingerprint helpery. Ověření: `get_title_presentation('tt0133093')` first hit `~0.12 s`, druhý hit `~0 s`.
- 2026-07-12: dočištěna i genre/app-state vrstva pro live PG runtime. Přibyly PG helpery `fetch_catalog_genres()`, `fetch_genre_score_source_rows()` a `fetch_relevant_people_candidate_rows()`; `get_catalog_genres()`, `get_genre_score_source_rows()`, `compute_and_record_genre_scores()`, `get_home_suggestion_sections()`, `get_genre_suggestion_candidates()` a `get_person_detail_cache_targets()` teď při plně-PG runtime nepoužívají DuckDB jen jako historickou mezivrstvu. Ověření: `get_catalog_genres()` `~0.57 s` (27 žánrů), `get_genre_score_source_rows()` `~1.13 s` (2445 titulů), `get_home_suggestion_sections(limit_per_section=2)` `~0.73 s`, `get_genre_suggestion_candidates('Action', limit=5)` `~0.62 s`, `compute_and_record_genre_scores()` `~1.62 s` (26 žánrů, 2445 titulů). Tím se běžný app runtime prakticky uzavírá jako PostgreSQL-first; zbylý DuckDB kód je už převážně fallback pro revert backendů, import preview/resolution a rebuild/ingest pracovní vrstva.
- 2026-07-12: chytrý IMDb ingest/rebuild pro PostgreSQL už není čistý full reload naslepo. `filmy/scripts/rebuild_catalog_postgresql.py` nově porovnává lokální TSV proti `app.imdb_file_manifest`, umí přeskočit úplně nezměněný běh a při změně reloaduje jen změněné `raw.*` zdroje; derived katalog (`app.catalog_*`, lookup tabulky) se pak přepočte jednou na konci jen pokud se opravdu něco změnilo. Přibyl i přepínač `--force` pro vědomý plný reload. Ověření: běh přes `uv run python -m filmy.scripts.rebuild_catalog_postgresql` na aktuálním stavu korektně vypsal všechny IMDb TSV jako `beze zmeny` a rebuild přeskočil. Tohle je záměrně file-level incremental sync, ne řádkový diff uvnitř obřích TSV; ten by byl další možný krok až pokud se ukáže, že reload změněného souboru je pořád příliš drahý.
- 2026-07-12: `System -> IMDb Refresh` už není jen UI nad starým DuckDB refreshem. Background job `filmy.scripts.run_imdb_refresh` po stažení, rozbalení a swapu `imdb/` nově volá přímo PostgreSQL rebuild entrypoint `rebuild_catalog_from_current_imdb(force=True)`; průběžné statusy v menu zůstaly zachované, jen se backendový krok přepsal z DuckDB na PostgreSQL a finální statistiky se berou z `app.catalog_titles`, `app.catalog_episodes`, `app.title_aliases`, `app.catalog_people` a `app.title_credits`. Ověření: `py_compile` pro `run_imdb_refresh.py` + `rebuild_catalog_postgresql.py` prošel a routa/menu odkaz dál míří na `/system/imdb-refresh`.
- 2026-07-12: další dočištění proti DuckDB proběhlo v živých servisních a import cestách. `POST /api/admin/imdb/rebuild` už nevolá `refresh_catalog()` nad DuckDB, ale stejný PostgreSQL rebuild entrypoint jako System menu. `get_catalog_stats()` při `runtime_catalog_backend = "postgres"` čte počty přímo z PostgreSQL. Hlavně `create_import_preview()` už v PostgreSQL režimu neotvírá DuckDB ani kvůli title/episode/alias lookupům: přibyly PG resolver helpery pro exact title match, `title_lookup`, `title_alias_lookup`, TMDB map i epizodní rozlišení. Současně byl odstraněn i zbytečný DuckDB open z watched-page PG větve. Ověření: `py_compile` prošel a read-only smoke nad PG resolverem vrátil `The Matrix -> tt0133093`.
- 2026-07-12: začalo odřezávání `db_legacy` od DuckDB. Do PostgreSQL migrací `002_runtime_schema.sql` a `003_runtime_grants.sql` bylo doplněno celé `old.*` schema (Trakt, IMDb lists, Plex), aby legacy syncy už nebyly blokované chybějící strukturou. První skutečně přepojená větev jsou `IMDb lists`: `sync_imdb_lists()`, `get_imdb_lists_status()`, `get_imdb_watchlist()` a `get_imdb_favorite_people()` v `filmy/db_legacy.py` teď běží nad PostgreSQL. Ověření: migrace `002` + `003` byly přímo aplikované přes `psql`, následný PG sync `imdb_lists/` doběhl `watchlist=502`, `favorite_people=65` a status/read smoke vrátil aktivní počty `502 / 65`.
- 2026-07-12: Plex větev v `filmy/db_legacy.py` je přepojená na PostgreSQL. `sync_plex_source()` a `get_plex_status()` už nepoužívají DuckDB connection, ale stejnou PG kompatibilní vrstvu jako Trakt (`_PgCompatConnection`). Zároveň byla doplněna ochrana, aby partial/test sync (`section_limit` nebo `item_limit_per_section`) nespouštěl cleanup neviděných položek a neblokoval další full sync přes fingerprint shortcut. Reálný full smoke proti Plex serveru `kulda_mini`: `sync_run_id=4c4e10b8-6b85-404b-b660-1dee870413c7`, `sections_processed=6`, `items_imported=1193`, `library_items_upserted=405`, `watch_events_upserted=100`, `content_state_updates=108`; status po doběhu ukazuje `active_library_items=1193`, `mapped_imdb_items=402`, `mapped_tmdb_items=399`, `watched_items=120`.
- 2026-07-12: startup `ensure_database()` už v PostgreSQL katalogovém režimu neotevírá DuckDB. Místo volání plného `rebuild_catalog_from_current_imdb()` při každém startu přibyl lehký PG startup check: ověří existenci `app.catalog_titles`, načte PG manifest/meta a rebuild pustí jen když opravdu chybí katalog nebo se změnily IMDb TSV zdroje. Současně byla opravena fail-closed kontrola v `filmy/scripts/bootstrap_postgresql.py`, aby povolovala i zamýšlené `USAGE` na schématu `old` pro `filmy_app`. Smoke: první hrubý běh po opravě ACL ještě trval ~16 s kvůli zbytečné re-aplikaci katalogového schématu; po dočištění lehkého startup checku `ensure_database()` doběhl v PG režimu za ~0.04 s.
- 2026-07-12: `filmy/db_library.py` prošel agresivním runtime cleanupem. Pro aktivní provozní cesty byly odstraněny DuckDB fallback větve u CRUD operací (`update_content_state`, watchlist/list CRUD, rating CRUD, watch-event CRUD, create/update list) i u hlavních listových read modelů (`get_hot_watchlist_page`, `get_local_library_status`, `get_continue_watching_items`, `get_user_list_items_page`). Modul teď pro tyto public entrypointy předpokládá PostgreSQL runtime backend a je výrazně čitelnější. Smoke bez DuckDB přístupu prošel pro `get_local_library_status()`, `get_hot_watchlist_page()`, `get_continue_watching_items()`, `get_user_list_items_page('watchlist')`, `set_watchlist_state('tt0133093', True)` a `clear_user_rating('tt0133093')`.
- 2026-07-12: další PG-only cleanup proběhl v `filmy/db.py` pro app-state a suggestion vrstvu. DuckDB fallbacky byly odstraněny z `get_favorite_genres()`, `get_favorite_traits()`, `get_genre_score_source_rows()`, `get_home_suggestion_sections()`, `get_genre_suggestion_candidates()`, `replace_favorite_genres()`, `replace_favorite_traits()`, `record_genre_score_snapshot()`, `compute_and_record_genre_scores()` a `get_latest_genre_scores()`. Současně `search_catalog()` v PG režimu už neotvírá DuckDB jen kvůli `library` summary, ale používá PG-aware `_fetch_library_summary(None, ...)`. Smoke s násilně shozeným `duckdb.connect` prošel pro favorite genres/traits, genre score rows, latest genre scores, homepage suggestions, genre candidates i `search_catalog('matrix', None, 2)`.
- 2026-07-12: TMDB helper blok v `filmy/db.py` je také PG-only. Odstraněny DuckDB fallbacky z `get_tmdb_mapping()`, `record_tmdb_asset()`, `get_latest_tmdb_assets()`, `get_tmdb_detail_locales()` a `get_latest_poster_records()`. Následný smoke s násilně shozeným `duckdb.connect` prošel pro `get_tmdb_mapping('tt0133093') -> tmdb_id 603`, `get_latest_tmdb_assets('tt0133093')`, `get_tmdb_detail_locales('tt0133093') -> ['en-US', 'cs-CZ']`, `get_latest_poster_records(['tt0133093'])` i `search_catalog('matrix', None, 1)`.
- 2026-07-12: navázal i `Trakt` legacy blok. Místo ručního přepisu všech SQL dotazů přibyla v `filmy/db_legacy.py` tenká PostgreSQL kompatibilní vrstva pro staré `?` placeholdery, přes kterou teď běží public Trakt sync/read cesty nad `old.trakt_*` v PostgreSQL: `sync_trakt_export()`, `get_trakt_sync_runs()`, `get_trakt_sync_run()`, `get_trakt_sync_changes()`, `get_trakt_ratings()`, `get_trakt_list_overview()`, `get_trakt_collection()` a `get_trakt_status()`. Ověření: PG sync `trakt-export/` doběhl `completed` s `107` soubory; status po syncu ukazuje `history_events_active=14157`, `ratings_active=234`, `lists_active=4`, `watchlist_active=236`, `collection_active=67`; detail runu a changes overview také prošly read smoke.

- 2026-07-01: do vyhledávání přidána interní recall/cache vrstva `app.search_recall`. Title i person lookup teď nejdřív zkouší malé mapování nedávných dotazů na konkrétní `tt...` / `nm...`, teprve potom pouští plné vyhledávání. Limit je v `config.toml` jako `search_recall_limit = 500`. Je to čistě technická optimalizace pro opakované hledání, ne uživatelská funkce a nemá mít vlastní UI.
- 2026-07-01: jemný refaktor `filmy/db.py` pokračoval vytažením bootstrap/seed/migrační logiky do nového modulu `filmy/db_bootstrap.py`. `filmy.db` zůstává fasáda, ale jednorázové kroky kolem archivace starých import tabulek, migrace legacy watch history, počátečního seedingu lokální knihovny a zrušení watched alias listu už jsou oddělené od běžného runtime kódu.
- 2026-07-01: výpočet `genre_scores` byl posunut na `genre-v2`. Nově do něj vstupuje i slabý signal oblibenosti hercu z `app.user_people.affinity_rating`, a to jen pres `app.title_credits` pro hlavni `cast` (top billing zhruba do 8. mista). Snapshoty si ukladaji i `actor_affinity_score`; jednorazovy skript pro prepocet si umi novy sloupec sam domigrovat i bez FastAPI startupu.
- 2026-07-01: homepage `Suggestions` už není jen diagnostika žánrů. Přibyly dva shortlisty: `Matches your traits` a `New on IMDb`. Traits se zatím vyhodnocují lehkým keyword matchingem nad TMDB overview a míchají se s `genre_scores`, IMDb quality a oblibeností herců. `New on IMDb` je lokální shortlist z katalogu, ne externí trending feed.
- 2026-06-30: search dostal skutečnou HTML výsledkovou stránku `GET /search` napojenou na horní navbar; stránka umí zobrazit jak nalezený titul, tak nalezenou osobu a shortlist alternativ.
- 2026-06-30: title lookup už nepreferuje lokálně viděné tituly; z rankingu zmizel `watched_count`, protože hledání musí fungovat i pro věci, které ještě nejsou v uživatelových seznamech ani historii.
- 2026-06-30: title search nově bere výrazněji v úvahu aliasy a normalizaci lokálních názvů; dotazy typu `3 procenta` se mapují i na názvy se znakem `%`.
- 2026-06-30: person search se rozšířil z pouhého full-name matchingu na víc variant jména. Fuzzy a levenshtein vrstva teď pracují i s příjmením a s kompaktní variantou bez mezer, takže jednoslovný překlep typu `Gylenhal` už najde `Jake Gyllenhaal`.
- 2026-06-30: person lookup už pro shortlist kandidátů netahá plnou filmografii každé osoby; místo toho používá levnější `credit_count` agregaci z `app.title_credits`. Další krok, pokud se k tomu vrátíme, je hlavně změřit rychlost celé search pipeline na reálných dotazech a případně ještě zúžit DB shortlist před Python fuzzy scoringem.
- 2026-06-30: aktivní Trakt integrace byla vyřazena z běžné appky. Admin endpointy a CLI export už nejsou součástí standardního provozu; export helper byl přesunut do `filmy.legacy.trakt_export` a historické `old.trakt_*` tabulky zůstávají jen jako archivní vrstva.
- 2026-06-30: FastAPI entrypoint byl rozdělen podle odpovědnosti. `filmy.main` je teď jen sestavení aplikace + `lifespan`, sdílené helpery jsou v `filmy.app_shared` a routy jsou rozdělené do `filmy.routers.web`, `filmy.routers.ui` a `filmy.routers.api`.
- 2026-06-30: proběhl krátký dokumentační pass nad novou FastAPI strukturou a složitější helper logikou. Breadcrumb/navigation helpery, cache warmup a nové person-affinity funkce mají doplněné vysvětlující docstringy, aby další změny nestály jen na implicitní znalosti chatu.
- 2026-06-29: detail titulu je znovu složený do tří vrstev pod hero blokem. `Main cast` je zpátky jako samostatný panel, `Directed by` a `Written by` mají vlastní nadpisy a badge jména, `Created by` je pod tím, aliasy jsou omezené jen na `en/cs/es/de` a nadpisy sekcí používají stejnou barvu jako název filmu.
- 2026-06-29: přidána druhá ruční preference vrstva `favorite_traits` pro volné výrazy typu `cerebral`, `dark` nebo `slow-burn`; je dostupná v `System` jako samostatná stránka a zatím slouží jen ke sběru explicitních preferencí bez navazující výpočetní logiky.
- 2026-07-01: stránka `System -> Favorite Traits` už není čistě volná. Má předvyplněný řízený seznam 30 doporučených traits a priorita je volitelná. Prázdná priorita znamená "ponechat v seznamu, ale zatím to neoznačovat jako oblíbené". Stávající ručně zadané hodnoty byly přepsány seed seznamem.
- 2026-07-01: ruční preference pro `favorite_genres` i `favorite_traits` jsou sjednocené na škálu `1-10`, kde nižší číslo znamená silnější preferenci. Stejné číslo může mít více položek. Prázdná hodnota znamená `nehodnoceno`, ne chybu.
- 2026-06-27: vytvořen sdílený `PLAN.md` v rootu projektu jako jedno místo pro další kroky a průběžné odškrtávání.
- 2026-06-27: doplněn důležitý směr pro enrichment. Neobohacujeme celý IMDb katalog, ale jen uživatelsky relevantní tituly a navazující entity vyvolané konkrétní akcí v appce.
- 2026-06-27: potvrzeno, že výchozí hledání titulů má stát na IMDb katalogu; TMDB je až navazující enrichment vrstva.
- 2026-06-27: upřesněno, že primární vazba titulu je přes IMDb `tconst`, zatímco TMDB může pomocně potvrdit existenci nebo dodat enrichment, ale nenahrazuje hlavní identitu.
- 2026-06-27: doplněno, že stejný princip platí i pro osoby: interní identita přes IMDb `nconst`, TMDB jen jako doplňková enrichment vrstva.
- 2026-06-27: zapsán návrhový princip budoucího vztahového pohledu nad databází, ale jen jako jedna z více možných UI vrstev.
- 2026-06-27: zapsán Plex jako doplňkový ingest zdroj, který má smysl zkusit připojit už v backendové fázi kvůli bohatším podkladům pro mapování a enrichment.
- 2026-06-27: ověřeno připojení na skutečný Plex server `kulda_mini`; detail metadat vrací GUID vazby na IMDb i TMDB, takže Plex může dodat použitelné mapovací podklady pro FILMY.
- 2026-06-27: upřesněno, že projekt se vyvíjí na MacBooku, ale cílově poběží na `mac-mini`, kde zároveň běží Plex; integraci je potřeba držet přenositelnou mezi stroji.
- 2026-06-27: první Plex sync doběhl do DB. Importováno 1193 Plex položek, z toho 402 s IMDb ID, 399 s TMDB ID; do `Plex Library` se propsalo 405 položek a vzniklo 100 `watch_events` ze stavu sledování.
- 2026-06-27: upraveno PyCharm datasource schema mapping v `.idea/dataSources.local.xml` na explicitní schémata `app`, `old`, `raw`, `main`, protože se vracela IDE hláška kolem `filmy.app.catalog_episodes.getImportedKeys` a duplicitních reportů. Další krok po restartu projektu: ověřit, jestli hláška zmizela; pokud ne, udělat čistý reinit datasource.
- 2026-06-27: protože hláška `filmy.app.catalog_episodes.getImportedKeys` přetrvala i po restartu, vyčištěna i PyCharm datasource historie a introspection cache v `.idea/dataSources/`; v databázi samotné `app.catalog_episodes` žádné foreign keys nemá, takže problém dál vypadá jako IDE metadata stav, ne DuckDB schéma.
- 2026-06-27: detail titulu už má funkční textový výstup nad vyhledáním podle názvu i `tconst`; IMDb osoby a kredity se nově materializují do `app.catalog_people` a `app.title_credits`, takže po výměně TSV dumpů v `imdb/` stačí znovu spustit `refresh_catalog()` a detail se přestaví nad novými daty.
- 2026-06-27: detail titulu má lokální cache v `data/assets/tmdb/<tconst>/detail.json`; při opakovaném zobrazení se bere z disku a přepíše se jen když se změní zdrojový fingerprint, takže běžný detail je rychlý a zároveň se dá znovu dopočítat po aktualizaci dat.
- 2026-06-27: rychlostní test ukázal, že cache detailu funguje, ale celkový dotaz stále brzdí vyhledávací vrstva; další optimalizace má proto cílit na fuzzy lookup a shortlist kandidátů.
- 2026-07-07: koncept projektu se zatím nezahazuje; výkonový problém není v identitě přes IMDb ani ve filesystem cache, ale hlavně v tom, že interaktivní search/UI cesta stále tahá příliš drahé lookup a enrichment kroky najednou.
- 2026-07-07: normální vyhledávání má být levné a tolerantní, ale ne za cenu automatického wide levenshtein fallbacku nad velkým katalogem; ten zůstává jen jako explicitní rozšířený režim.
- 2026-06-27: přidán malý runner `filmy-materialize-title-details`, který projde jen kompletní tituly a zapíše jim `data/assets/tmdb/<tconst>/detail.json`; použitelné pro jednorázové doplnění cache nebo pozdější refresh.
- 2026-06-28: domovka má limit 50 položek pro vybraný seznam a odkazy `Show all` vedou na samostatnou stránku seznamu; detail prezentace titulu je navíc cacheovaný v procesu přes bounded `lru_cache`, s invalidací po zápisech do lokalních dat.
- 2026-06-27: materializace `detail.json` už neběží jen nad perfektně kompletními TMDB záznamy, ale nad všemi relevantními tituly; existující cache se validuje přes fingerprint místo slepého přeskočení podle existence souboru.
- 2026-06-27: TMDB backfill nově umí označit `not_found` tituly a vyřadit je z dalších batchů, takže se nemá opakovaně zasekávat na stejných IMDb ID, která v TMDB nejsou.
- 2026-06-27: do appky přidán interní background supervisor. Při startu FastAPI spouští TMDB backfill a materializaci `detail.json`, hlídá jejich liveness podle procesu a log activity a po pádu nebo zaseknutí je znovu nahodí.
- 2026-06-27: přidán lookup flow `GET /api/catalog/lookup`, který pro dotaz na název vrací rovnou vybraný detail titulu a zároveň shortlist kandidátů pro budoucí UI přepnutí mezi shodami.
- 2026-06-27: při budování Jinja2 vyhledávacího UI zachovat tolerantní hledání na překlepy typu `tennet` -> `Tenet`; uživatel nemá být závislý jen na přesném názvu.
- 2026-06-27: později doplnit testy pro fuzzy lookup a doladění hran podobnosti; současný kód pro tolerantní hledání už existuje a je funkční.
- 2026-06-27: po znovuotevření projektu a nové introspekci se duplicitní hláška v PyCharm objevuje dál; protože běh appky ani DB logika nejsou blokované, evidujeme to zatím jen jako odložený IDE-only technický dluh k pozdějšímu dořešení.
- 2026-06-27: zpřehledněn TMDB backfill runner. Nově loguje průběh po jednotlivých titulech místo tichého čekání jen na konec batch, takže je vidět, kde přesně se enrichment zpomaluje nebo selhává.
- 2026-06-27: zpevněna TMDB enrichment vrstva. Mapping se neoznačuje jako `synced` před uložením payloadů, `/configuration` se v procesu kešuje, asset fetch umí vrátit částečný výsledek s per-asset chybami a retry vrstva zachytává i timeouty / nízkoúrovňové I/O chyby.
- 2026-06-27: repo prošlo prvním strukturálním úklidem směrem k běžné FastAPI appce. Hlavní logika je v balíčku `filmy/`, integrace jsou v `filmy/integrations/`, runner skripty v `filmy/scripts/` a root obsahuje už jen tenké vstupní wrappery a projektové soubory.
- 2026-06-27: proběhl druhý úklidový pass nad artefakty v rootu. Dočasné `tmp_trakt*.json` přesunuty do `tmp/`, smazány cache a `.DS_Store`, a `.gitignore` doplněn o generované debug/probe soubory a lokální pracovní adresáře.
- 2026-06-27: ověřeno, že TMDB backfill runner sám o sobě umí běžet stabilně, ale detached start přes AI exec prostředí nebyl spolehlivý. Až bude UI, je potřeba výslovně otestovat běh bez AI orchestrace: normální proces, interní worker nebo systémový launcher, a potvrdit, že background enrichment funguje stabilně i v reálném provozu appky.
- 2026-06-28: doplněny lokální user listy o `description`, minimalistické vytvoření nového listu přes `+`, a inline `Edit` pro popis aktivního seznamu vpravo.
- 2026-06-28: `Watched` v kartovém menu je nyní explicitně navázané na seznam `Viděl jsem`; akce titul odebere z aktivního seznamu a zároveň ho přidá do watched alias listu.
- 2026-06-29: lokální alias seznam `Viděl jsem` byl zrušen jako samostatný user list; místo něj je nově systémový pohled `Watched` odvozený přímo z `watch_events`, zatímco `Recently Watched` zůstává jen časově omezený výřez nad stejnou historií.
- 2026-06-29: u uživatelských seznamů nově držíme doménové rozlišení mezi budoucím `Move to` a `Copy to`; například `Watchlist` -> `Koukni rychle` nebo `Mam` nemá automaticky znamenat odebrání z původního seznamu.
- 2026-06-29: lokální popover menu na kartách seznamů nově používá dvoukrokový flow `Move to` / `Copy to`; hlavní menu zůstává krátké a cílové seznamy se ukazují až ve druhém kroku, bez nabídky aktuálního seznamu.
- 2026-06-29: při hromadném refreshi stale title cache jsme znovu narazili na write lock kolizi nad `filmy.duckdb`; potvrzený technický dluh je potřeba sjednotit write model a nepouštět více nezávislých writerů paralelně.
- 2026-06-28: lokální nabídka subcard už není bootstrap/details varianta; používá nativní `popover`, protože scroll panel watchlistu nabídku jinak ořezával.
- 2026-06-28: vizuálně ověřeno na skutečném renderu domovky, že aktuální výška pravé sekce `watchlist` je spodní použitelná hranice; další úpravy ji mohou zvětšit, ale ne zmenšit.
- 2026-06-28: `config.toml` je zdroj pravdy pro limity `continue_watching_limit` a `my_lists_selected_limit`; změna souboru se má propsat bez další úpravy kódu.
- 2026-06-28: v domovce je pravý panel s vybraným seznamem dorovnaný na výšku `Your Lists`, ale současně nesmí klesnout pod ověřené minimum.
- 2026-06-30: geometrie homepage seznamů se má řídit obsahem levého panelu `Your Lists`; pravý panel se zvoleným seznamem se na desktopu dorovnává na stejnou výšku, ale minimálně musí pojmout zhruba 3 řady filmových karet.
- 2026-06-30: homepage sekce nahoře s `Your Lists` a vybraným seznamem je po doladění schválená a má se brát jako zmrazená proti dalším úpravám; výjimkou je jen samostatná sekce `Suggestions`, kterou lze dál měnit odděleně.
- 2026-06-30: `metadata_pipeline` už nemá běžet agresivně každé 2 sekundy i bez práce. Nově má aktivní krátký sleep jen při skutečném postupu, jinak přechází do idle wait režimu s delší periodickou kontrolou a probuzením přes signal soubor po write akcích z UI/API.
- 2026-06-30: background provoz je zjednodušený na `metadata_pipeline` + `background supervisor`. Staré `tmdb_backfill`, `title_details_cache` rewrite a `refresh_stale_title_caches` se mají brát už jen jako archivní servisní nástroje, ne jako běžné background procesy.
- 2026-06-30: přidána první verze `System > IMDb Refresh`. Je to jednorázový servisní subprocess se stavem a logem, který stáhne IMDb TSV dumpy bokem, rozbalí je do `data/imdb_refresh/<timestamp>/`, krátce přepne aktivní `imdb/` a pak spustí standardní `refresh_catalog()`.
- 2026-06-28: pro osoby vzniká stejný materializovaný detail princip jako pro tituly: lokální `detail.json` pod `data/assets/people/<nconst>/detail.json`, klíčovaný přes IMDb `nconst`, aby budoucí UI detail osoby nečekal na skládání dat za běhu.
- 2026-06-28: metadata pipeline nově po TMDB a title detail cache průběžně materializuje i person `detail.json`, takže i detaily lidí se mají doplňovat na pozadí stejným supervised loopem.
- 2026-06-28: background pipeline nově dohledává i TMDB portréty lidí přes IMDb `nconst` a ukládá je do `data/assets/people/<nconst>/portrait.*`; navazující person `detail.json` pak umí hned nést `portrait_url`.
- 2026-06-28: detail titulu nově zobrazuje u `Main cast` malé portréty z lokálních person assetů; při otevření detailu se chybějící portréty herců dohledávají přednostně na pozadí.
- 2026-06-28: `Main cast` se po otevření detailu průběžně sám obnovuje malým lokálním partial refreshem, takže nově stažené portréty naskakují bez ručního reloadu celé stránky.
- 2026-06-29: `Main cast` chipy na detailu titulu nově vedou na HTML detail osoby `/people/{nconst}`; detail osoby se skládá z materializovaných person dat a filmografie a linkuje zpět do detailů titulů přes IMDb identitu bez dalšího fuzzy lookup mezikroku.
- 2026-06-30: horní `Back` navigace je nahrazená delšími breadcrumbs. Stopa se nově přenáší v URL mezi homepage/listy, detailem titulu a detailem osoby, takže se dá vracet i přes několik mezikroků zpět bez rozbití layoutu.
